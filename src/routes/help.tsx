import { createFileRoute } from "@tanstack/react-router";
import { useState } from "react";
import { ChevronDown, MessageSquare } from "lucide-react";
import { SessionGuard } from "@/components/SessionGuard";
import { StudentShell } from "@/components/StudentShell";
export const Route = createFileRoute("/help")({ component: () => <SessionGuard><HelpPage /></SessionGuard> });
const faq = [["How do I start a course?", "Open Courses, choose a specialization, then select a lesson from your dashboard."], ["How is my progress tracked?", "Lessons are marked as you complete them. Your dashboard shows completion by course."], ["When can I download a certificate?", "A certificate unlocks after every lesson in that niche is completed."], ["How do I contact support?", "Use the Messages button in the top bar to send the LearnHub PH team a message."]];
function HelpPage() { const [open, setOpen] = useState(0); return <StudentShell title="Help Center"><div className="max-w-3xl"><h2 className="text-2xl font-black">How can we help?</h2><p className="mt-1 text-sm text-gray-500">Quick answers and guides for your LearnHub PH learning journey.</p><section id="faq" className="mt-7 rounded-2xl bg-white p-5 shadow-sm"><h3 className="font-black">Frequently asked questions</h3><div className="mt-3 divide-y">{faq.map(([q,a], i) => <div key={q}><button onClick={() => setOpen(open === i ? -1 : i)} className="flex w-full items-center justify-between py-4 text-left text-sm font-bold"><span>{q}</span><ChevronDown size={18} className={open === i ? "rotate-180" : ""} /></button>{open === i && <p className="pb-4 text-sm leading-6 text-gray-500">{a}</p>}</div>)}</div></section><section className="mt-5 flex items-center gap-4 rounded-2xl bg-purple-700 p-5 text-white"><MessageSquare /><div><h3 className="font-black">Still need help?</h3><p className="text-sm text-purple-100">Send support a message from the message icon in the top bar.</p></div></section></div></StudentShell>; }
